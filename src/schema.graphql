type Query {
  info: String!
  feed: [Link!]!
  link(id: Int!): Link!
}

type Mutation {
  post(url: String!, description: String!): Link!
  add(url: String!, description: String!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}